{
  "name": "figma-library-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "frontend-nextjs"
  ],
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:next": "npm run dev --prefix frontend-nextjs",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:next": "npm run build --prefix frontend-nextjs"
  }
}
